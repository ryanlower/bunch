#!/usr/bin/env ruby

$:.unshift File.expand_path('../../lib', __FILE__)
require 'bunch'

path = ARGV.shift
opt  = ARGV.shift

Bunch.extension = case
  when opt
    opt.sub(/-+/, '')
  else
    'js'
  end

puts Bunch.concatenate(path)
